import { HeaderContainer } from "./Header.styles";

function Header() {
  return (
    <HeaderContainer>
      <h1 className="header">Fun with GPT-3</h1>
    </HeaderContainer>
  );
}

export default Header;
